{
  "name": "agg-rust-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run server.ts",
    "build:wasm": "powershell -File build-wasm.ps1",
    "build": "bun run build.ts",
    "test": "bun test tests/",
    "test:e2e": "bun run tests/repro.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "happy-dom": "^20.11.0",
    "playwright": "^1.58.2"
  }
}
